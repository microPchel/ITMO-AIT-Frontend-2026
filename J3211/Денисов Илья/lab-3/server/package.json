{
  "name": "task-search-json-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "json-server --watch db.json --port 3001",
    "server": "json-server --watch db.json --port 3001"
  },
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
